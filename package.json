{
  "name": "explore_Travis",
  "version": "1.0.0",
  "description": "Trying to create a work env",
  "scripts": {
    "lint": "npx eslint date.js",
    "test": "node test.js",
    "build": "echo 'building locally'",
    "now-build": "echo 'building on production' ",
    "deploy": "now alias --token $TOKEN $(echo $(now deploy --token $TOKEN)) exploretravis-$BRANCH.now.sh && echo $BRANCH",
    "start": "node date.js"
  },
  "author": "Michael Markelov",
  "license": "ISC",
  "dependencies": {
    "now": "^14.1.0"
  }
}
